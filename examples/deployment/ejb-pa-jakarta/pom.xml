<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>org.eximeebpms.bpm.quickstart</groupId>
  <artifactId>eximeebpms-quickstart-ejb-pa-jakarta</artifactId>
  <packaging>war</packaging>
  <name>EximeeBPMS - Quickstarts - Jakarta Ejb Process Application</name>
  <version>1.0-SNAPSHOT</version>

  <properties>
    <version.eximeebpms>1.3.0</version.eximeebpms>
    <version.jakarta-ee-specs>11.0.0</version.jakarta-ee-specs>
    <version.maven-compiler-plugin>3.14.1</version.maven-compiler-plugin>
    <version.maven-war-plugin>3.4.0</version.maven-war-plugin>
    <maven.compiler.release>17</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <failOnMissingWebXml>false</failOnMissingWebXml>
  </properties>

  <dependencies>

    <dependency>
      <groupId>org.eximeebpms.bpm</groupId>
      <artifactId>eximeebpms-engine</artifactId>
      <version>${version.eximeebpms}</version>
      <scope>provided</scope>
    </dependency>

    <dependency>
      <groupId>org.eximeebpms.bpm.javaee</groupId>
      <artifactId>eximeebpms-ejb-client-jakarta</artifactId>
      <version>${version.eximeebpms}</version>
    </dependency>

    <dependency>
      <groupId>org.eximeebpms.bpm</groupId>
      <artifactId>eximeebpms-engine-cdi-jakarta</artifactId>
      <version>${version.eximeebpms}</version>
    </dependency>

    <dependency>
      <!-- Jakarta EE Specification -->
      <groupId>jakarta.platform</groupId>
      <artifactId>jakarta.jakartaee-api</artifactId>
      <version>${version.jakarta-ee-specs}</version>
      <type>pom</type>
      <scope>provided</scope>
    </dependency>

  </dependencies>



  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-war-plugin</artifactId>
        <version>${version.maven-war-plugin}</version>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <version>${version.maven-compiler-plugin}</version>
      </plugin>
    </plugins>
  </build>

</project>
